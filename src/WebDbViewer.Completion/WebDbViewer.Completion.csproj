<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\WebDbViewer.Core\WebDbViewer.Core.csproj" />
    <ProjectReference Include="..\WebDbViewer.Parsing\WebDbViewer.Parsing.csproj" />
    <ProjectReference Include="..\WebDbViewer.Metadata\WebDbViewer.Metadata.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Грамматический анализ и обрезка до текущего statement — internal, но проверяются напрямую:
         через публичный CompletionEngine их поломка не отличима от исправной работы. -->
    <InternalsVisibleTo Include="WebDbViewer.Tests.Unit" />
  </ItemGroup>

</Project>
